import 'dart:ui';

import 'package:flutter/cupertino.dart';

class ScrollDownToClose extends StatelessWidget {
  const ScrollDownToClose({
    Key key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return null;
  }
}
